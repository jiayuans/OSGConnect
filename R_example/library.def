Bootstrap: docker
From: rccline/lme4verse:latest

%post
    apt -y update
    apt -y install jags
    R -e "install.packages(c('ggplot2','lme4'), dependencies=TRUE, repos='http://cran.rstudio.com/')"
